using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMovementTest : MonoBehaviour
{
    public float currentSpeed; // hvor hurtigt billen køre lige nu... 
    public float minSpeed; // bilens min speed. 
    public float MaxSpeed; // bilens max speed. 
    public float speed => currentSpeed; // for at kunne læse nuværende fart fra  script = collisionDamage &&  ! 
    // Start is called before the first frame update
    void Start()
    { 
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
